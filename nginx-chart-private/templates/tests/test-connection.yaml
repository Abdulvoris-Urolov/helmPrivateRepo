apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nginx-chart-private.fullname" . }}-test-connection"
  labels:
    {{- include "nginx-chart-private.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nginx-chart-private.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
